import { View, Text } from "react-native";

export default function ForgotPassword() {
  return (
    <View className="flex-row justify-center">
      <Text className={`text-base font-bold ml-1 text-white mt-4`}>
        Esqueci minha senha
      </Text>
    </View>
  );
}
